import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/server/**/*.test.ts"],
    // Run the file-writing end-to-end test serially-safe; defaults are fine.
  },
});
