diff --git a/src/server/__tests__/auditFailClosed.test.ts b/src/server/__tests__/auditFailClosed.test.ts
index 5ef61bd..0a5e9c0 100644
--- a/src/server/__tests__/auditFailClosed.test.ts
+++ b/src/server/__tests__/auditFailClosed.test.ts
@@ -52,18 +52,16 @@ describe("audit-chain corruption", () => {
     const pid = 2_147_483_647;
     await mkdir(queue, { recursive: true });
     await writeFile(
-      resolve(queue, `owner.000000000001.${pid}.${token}.json`),
+      resolve(queue, `owner.${pid}.${token}.json`),
       JSON.stringify({
         version: 1,
-        phase: "owner",
         pid,
-        acquiredAt: Date.now() - 60_000,
         createdAt: Date.now() - 60_000,
         token,
-        ticket: 1,
       }),
       "utf8",
     );
+    await writeFile(resolve(queue, "tickets.log"), `${token}\n`, "utf8");
 
     await expect(
       appendAuditEvent({ type: "run.queued", runId: "run-after-crash" }),
@@ -75,7 +73,7 @@ describe("audit-chain corruption", () => {
     const queue = resolve(dataRoot, "audit", ".append.lock.owners");
     const contender = resolve(
       queue,
-      "choosing.2147483647.00000000-0000-4000-8000-000000000062.json",
+      "owner.2147483647.00000000-0000-4000-8000-000000000062.json",
     );
     await mkdir(queue, { recursive: true });
     await writeFile(contender, '{"version":', "utf8");
diff --git a/src/server/__tests__/factoryCliContract.test.ts b/src/server/__tests__/factoryCliContract.test.ts
index 85d11eb..2bbaac6 100644
--- a/src/server/__tests__/factoryCliContract.test.ts
+++ b/src/server/__tests__/factoryCliContract.test.ts
@@ -16,7 +16,7 @@ describe("Factory Deck cloud CLI terminal contract", () => {
     expect(source).toContain("Run was cancelled.");
     expect(source).toContain("Run completed without a final report");
     expect(source).toContain("process.env.FACTORY_PROJECT_ID?.trim()");
-    expect(source).toContain('args.includes("--demo")');
+    expect(source).toContain("parseFactoryCliInputs(process.argv)");
     expect(source).toContain("demo: true, publish: false, pushToOrigin: false");
     expect(source).toContain("OFFLINE DEMO COMPLETE");
     expect(source).toContain("never delivered or production-ready");
diff --git a/src/server/__tests__/processFileLock.test.ts b/src/server/__tests__/processFileLock.test.ts
index c7737c2..f8b14f0 100644
--- a/src/server/__tests__/processFileLock.test.ts
+++ b/src/server/__tests__/processFileLock.test.ts
@@ -41,7 +41,7 @@ describe("cross-process file lock", () => {
     expect(maximumActive).toBe(1);
     expect(order).toHaveLength(20);
     const queue = _processLockQueuePathForTests(lockPath);
-    await expect(readdir(queue)).resolves.toEqual([]);
+    await expect(readdir(queue)).resolves.toEqual(["tickets.log"]);
   });
 
   it("keeps a later contender out until the current owner releases", async () => {
@@ -69,22 +69,18 @@ describe("cross-process file lock", () => {
     await mkdir(queue, { recursive: true });
     const token = "00000000-0000-4000-8000-000000000051";
     const pid = 2_147_483_647;
-    const path = resolve(
-      queue,
-      `owner.000000000001.${pid}.${token}.json`,
-    );
+    const path = resolve(queue, `owner.${pid}.${token}.json`);
     await writeFile(
       path,
       JSON.stringify({
         version: 1,
-        phase: "owner",
         pid,
         createdAt: Date.now() - 60_000,
         token,
-        ticket: 1,
       }),
       "utf8",
     );
+    await writeFile(resolve(queue, "tickets.log"), `${token}\n`, "utf8");
 
     const lease = await acquireProcessFileLock(lockPath, {
       timeoutMs: 1_000,
@@ -93,33 +89,36 @@ describe("cross-process file lock", () => {
     });
     expect(lease).not.toBeNull();
     await lease!.release();
-    await expect(readdir(queue)).resolves.toEqual([]);
+    await expect(readdir(queue)).resolves.toEqual(["tickets.log"]);
   });
 
-  it("fails closed on a recent malformed contender and clears it only after grace", async () => {
+  it("does not treat an unticketed partial owner as a lease and clears it after grace", async () => {
     const queue = _processLockQueuePathForTests(lockPath);
     await mkdir(queue, { recursive: true });
-    const malformed = resolve(
-      queue,
-      "choosing.2147483647.00000000-0000-4000-8000-000000000052.json",
-    );
+    const malformedName =
+      "owner.2147483647.00000000-0000-4000-8000-000000000052.json";
+    const malformed = resolve(queue, malformedName);
     await writeFile(malformed, '{"version":', "utf8");
 
-    await expect(
-      acquireProcessFileLock(lockPath, {
-        timeoutMs: 10,
-        pollMs: 1,
-        staleGraceMs: 60_000,
-      }),
-    ).resolves.toBeNull();
+    const first = await acquireProcessFileLock(lockPath, {
+      timeoutMs: 1_000,
+      pollMs: 1,
+      staleGraceMs: 60_000,
+    });
+    expect(first).not.toBeNull();
+    await first!.release();
+    await expect(readdir(queue).then((names) => names.sort())).resolves.toEqual([
+      malformedName,
+      "tickets.log",
+    ]);
 
-    const lease = await acquireProcessFileLock(lockPath, {
+    const second = await acquireProcessFileLock(lockPath, {
       timeoutMs: 1_000,
       pollMs: 1,
       staleGraceMs: 0,
     });
-    expect(lease).not.toBeNull();
-    await lease!.release();
-    await expect(readdir(queue)).resolves.toEqual([]);
+    expect(second).not.toBeNull();
+    await second!.release();
+    await expect(readdir(queue)).resolves.toEqual(["tickets.log"]);
   });
 });
diff --git a/src/server/storage/processFileLock.ts b/src/server/storage/processFileLock.ts
index d090c9b..6f670fb 100644
--- a/src/server/storage/processFileLock.ts
+++ b/src/server/storage/processFileLock.ts
@@ -4,23 +4,20 @@ import { lstat, mkdir, open, readdir, rm } from "node:fs/promises";
 import { resolve } from "node:path";
 
 /**
- * Cross-process file lock implemented as a Lamport-style bakery queue.
+ * Crash-recoverable cross-process file lock.
  *
- * Every contender owns unique, unguessable files. No process ever unlinks a
- * shared lock pathname, so reclaiming an abandoned contender cannot delete a
- * replacement owner that arrived after a stale snapshot. The short-lived
- * `choosing` file closes the ticket-selection race; the persisted ticket plus
- * UUID gives every live contender a total order.
+ * Contenders never share an owner pathname. Each creates a UUID-bound owner
+ * receipt, then appends that UUID to an immutable ticket log in one O_APPEND
+ * write. The append order is the ownership order. New arrivals therefore
+ * cannot jump ahead of a waiter, and stale recovery only removes the abandoned
+ * UUID pathname, never a replacement owner that appeared after inspection.
  */
 
 const HAS_NOFOLLOW = typeof FS.O_NOFOLLOW === "number" && FS.O_NOFOLLOW !== 0;
 const UUID_PATTERN =
   /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
-const CHOOSING_PATTERN =
-  /^choosing\.(\d+)\.([0-9a-f-]{36})\.json$/i;
-const OWNER_PATTERN =
-  /^owner\.(\d{12})\.(\d+)\.([0-9a-f-]{36})\.json$/i;
-const MAX_SAFE_TICKET = 999_999_999_999;
+const OWNER_PATTERN = /^owner\.(\d+)\.([0-9a-f-]{36})\.json$/i;
+const TICKET_LOG = "tickets.log";
 
 export type ProcessFileLease = {
   token: string;
@@ -33,40 +30,38 @@ export type ProcessFileLockOptions = {
   staleGraceMs?: number;
 };
 
-type ContenderReceipt = {
+type OwnerReceipt = {
   version: 1;
-  phase: "choosing" | "owner";
   pid: number;
   createdAt: number;
   token: string;
-  ticket: number | null;
 };
 
-type Contender = {
+type ActiveOwner = OwnerReceipt & {
   path: string;
-  phase: "choosing" | "owner";
-  pid: number;
-  token: string;
-  ticket: number | null;
-  createdAt: number;
+  ticket: number;
 };
 
-type ScanResult = {
-  choosing: Contender[];
-  owners: Contender[];
-  uncertain: boolean;
+type TicketCache = {
+  identity: string;
+  offset: number;
+  carry: string;
+  nextTicket: number;
+  order: Map<string, number>;
 };
 
+const ticketCaches = new Map<string, TicketCache>();
+
 function queuePath(lockPath: string): string {
   return `${resolve(lockPath)}.owners`;
 }
 
-function choosingName(pid: number, token: string): string {
-  return `choosing.${pid}.${token}.json`;
+function ownerName(pid: number, token: string): string {
+  return `owner.${pid}.${token}.json`;
 }
 
-function ownerName(ticket: number, pid: number, token: string): string {
-  return `owner.${String(ticket).padStart(12, "0")}.${pid}.${token}.json`;
+function ticketLogPath(queue: string): string {
+  return resolve(queue, TICKET_LOG);
 }
 
 function processIsAlive(pid: number): boolean {
@@ -97,10 +92,7 @@ async function ensureQueue(path: string): Promise<void> {
   }
 }
 
-async function writeExclusiveReceipt(
-  path: string,
-  receipt: ContenderReceipt,
-): Promise<void> {
+async function writeExclusiveOwner(path: string, receipt: OwnerReceipt): Promise<void> {
   const flags =
     FS.O_WRONLY | FS.O_CREAT | FS.O_EXCL | (HAS_NOFOLLOW ? FS.O_NOFOLLOW : 0);
   const handle = await open(path, flags, 0o600);
